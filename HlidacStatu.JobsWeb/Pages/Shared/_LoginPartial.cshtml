@using HlidacStatu.Entities
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<div class="navbar-nav ms-auto">
    @if (SignInManager.IsSignedIn(User))
    {
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Nastavení účtu"><i class="fas fa-user"></i></a>
        <a class="nav-link btn btn-link text-dark" href="/Identity/Account/Logout?retUrl_2=/" title="Odhlášení">Odhlásit</a>
    }
    else
    {
        <a class="nav-link" href="/Identity/Account/Login?retUrl_2=/"><img src="/content/img/icon-user.svg" style="padding-right: 8px;">Přihlášení</a>
        @*<a class="nav-link" href="/Identity/Account/Register?retUrl_2=/">Registrace</a>*@
    }
</div>