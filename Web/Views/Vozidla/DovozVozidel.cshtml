@model HlidacStatu.RegistrVozidel.Models.ImportYearReport
@using System.Globalization
@{
    ViewBag.Title = "Dovoz vozidel" + (Model.SelectedYear.HasValue ? $" - {Model.SelectedYear}" : "");
    ViewBag.SubTitle = "Registr vozidel";
}

@section breadcrumb
{
    <ol class="breadcrumb">
        <li><a href="/">Hlídač Státu</a></li>
        <li><a href="/vozidla">Registr vozidel</a></li>
        <li><a href="/vozidla/Statistiky">Statistiky</a></li>
        <li class="active">Dovoz vozidel</li>
    </ol>
}

<h3 class="mt-2">
    <i class="fa-solid fa-plane-arrival me-2"></i>Dovoz osobních automobilů podle země původu @(Model.SelectedYear.HasValue ? $"v roce {Model.SelectedYear}" : "za všechny roky")
</h3>
<p class="text-muted">Top 30 zemí, ze kterých byla dovezena vozidla.</p>

<form method="get" class="d-flex align-items-center gap-3 mb-3 flex-wrap">
    <div class="d-flex align-items-center gap-2">
        <label for="rok" class="form-label mb-0">Rok dovozu:</label>
        <select name="rok" id="rok" class="form-select d-inline-block w-auto" onchange="this.form.submit()">
            <option value="">Všechny roky</option>
            @foreach (var y in Model.AvailableYears)
            {
                if (y == Model.SelectedYear)
                {
                    <option value="@y" selected="selected">@y</option>
                }
                else
                {
                    <option value="@y">@y</option>
                }
            }
        </select>
    </div>
    <div class="form-check">
        <input class="form-check-input" type="checkbox" name="statniOnly" value="true" id="statniOnly"
               @(((bool?)ViewBag.StatniOnly == true) ? "checked" : "") onchange="this.form.submit()" />
        <label class="form-check-label" for="statniOnly">Pouze státní organizace</label>
    </div>
</form>

@if (!Model.Items.Any())
{
    <div class="alert alert-info mt-3">Žádná data o dovozu @(Model.SelectedYear.HasValue ? $"pro rok {Model.SelectedYear}" : "").</div>
}
else
{
    int totalImports = Model.Items.Sum(i => i.Count);
    <p class="mb-3"><strong>Celkem dovezených vozidel:</strong> @totalImports.ToString("N0", CultureInfo.GetCultureInfo("cs-CZ"))</p>

    <div class="table-responsive mt-3">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Země</th>
                    <th class="text-end">Počet</th>
                    <th class="text-end">Podíl</th>
                    <th style="min-width:200px"></th>
                </tr>
            </thead>
            <tbody>
                @{ int rank = 1; }
                @foreach (var item in Model.Items)
                {
                    double percent = totalImports > 0 ? (double)item.Count / totalImports * 100 : 0;
                    <tr>
                        <td>@rank</td>
                        <td><strong>@(string.IsNullOrWhiteSpace(item.Country) ? "-" : item.Country)</strong></td>
                        <td class="text-end"><strong>@item.Count.ToString("N0", CultureInfo.GetCultureInfo("cs-CZ"))</strong></td>
                        <td class="text-end">@percent.ToString("N1", CultureInfo.GetCultureInfo("cs-CZ")) %</td>
                        <td>
                            <div class="progress" style="height: 1.2rem;">
                                <div class="progress-bar bg-primary" role="progressbar"
                                     style="width: @percent.ToString("F1", CultureInfo.InvariantCulture)%"
                                     aria-valuenow="@percent.ToString("F1", CultureInfo.InvariantCulture)"
                                     aria-valuemin="0" aria-valuemax="100">
                                </div>
                            </div>
                        </td>
                    </tr>
                    rank++;
                }
            </tbody>
        </table>
    </div>
}
